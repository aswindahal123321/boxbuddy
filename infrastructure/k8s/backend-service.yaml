apiVersion: v1
kind: Service
metadata:
  name: freshplate-backend
  labels:
    app: freshplate-backend
spec:
  type: LoadBalancer
  selector:
    app: freshplate-backend
  ports:
    - name: http
      port: 80
      targetPort: 8080
